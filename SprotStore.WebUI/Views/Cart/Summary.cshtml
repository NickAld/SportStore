@model SportsStore.Domain.Entities.Cart

<div id="cart"> 
    <span class="caption">
        <b>Ваша корзина</b>
        @Model.Lines.Sum(x=>x.Quantity) товаров(ов)
        <br />
        @Model.ComputeTotalValue()
    </span>
    @Html.ActionLink("Checkout", "Index", "Cart", new { returnUrl = Request.Url.PathAndQuery }, null)
</div>